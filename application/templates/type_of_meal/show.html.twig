{% extends 'base.html.twig' %}

{% block title %}TypeOfMeal{% endblock %}

{% block body %}
    <h1>TypeOfMeal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_meal.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_of_meal.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ type_of_meal.status ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>DisplayOrder</th>
                <td>{{ type_of_meal.displayOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_of_meal_index') }}">back to list</a>

    <a href="{{ path('app_type_of_meal_edit', {'id': type_of_meal.id}) }}">edit</a>

    {{ include('type_of_meal/_delete_form.html.twig') }}
{% endblock %}
