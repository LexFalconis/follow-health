<form method="post" action="{{ path('app_diabetes_delete', {'id': diabetes.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diabetes.id) }}">
    <button class="btn">Delete</button>
</form>
