<form method="post" action="{{ path('app_satisfaction_with_food_delete', {'id': satisfaction_with_food.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ satisfaction_with_food.id) }}">
    <button class="btn">Delete</button>
</form>
