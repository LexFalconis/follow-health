{% extends 'base.html.twig' %}

{% block title %}SatisfactionWithFood{% endblock %}

{% block body %}
    <h1>SatisfactionWithFood</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ satisfaction_with_food.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ satisfaction_with_food.description }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ satisfaction_with_food.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_satisfaction_with_food_index') }}">back to list</a>

    <a href="{{ path('app_satisfaction_with_food_edit', {'id': satisfaction_with_food.id}) }}">edit</a>

    {{ include('satisfaction_with_food/_delete_form.html.twig') }}
{% endblock %}
